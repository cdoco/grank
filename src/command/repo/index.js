import { neonGreen } from '../../utils/chalk';
import { cfonts } from '../../utils/cfonts';

const repo = (query, option) => {

    //title
    cfonts('Github Rank');

    console.log(neonGreen('To achieve.'));
};

export default repo;